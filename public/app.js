const form=document.querySelector("#form"),status=document.querySelector("#status"),answer=document.querySelector("#answer");
form.addEventListener("submit",async(event)=>{event.preventDefault();status.textContent="Running the benchmark…";answer.hidden=true;try{const response=await fetch("/api/evaluate",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({repository:document.querySelector("#repository").value,ref:"main"})}),report=await response.json();if(!response.ok)throw new Error(report.error||"Evaluation failed.");answer.textContent=report.recommendation_text;answer.hidden=false;status.textContent="Recommendation ready.";}catch(error){status.textContent=error.message;}});
