import { ConfigurationError, executeEvaluation } from "../../src/evaluation-service.mjs";
import { ClientError } from "../../src/workflow.mjs";
const json=(statusCode,body)=>({statusCode,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"},body:JSON.stringify(body)});
export async function handler(event){if(event.httpMethod==="OPTIONS")return{statusCode:204,body:""};if(event.httpMethod!=="POST")return json(405,{error:"Method not allowed."});try{return json(200,await executeEvaluation(JSON.parse(event.body||"{}")));}catch(error){if(error instanceof SyntaxError)return json(400,{error:"Malformed JSON body."});if(error instanceof ClientError)return json(error.status,{error:error.message});if(error instanceof ConfigurationError)return json(503,{error:error.message});return json(502,{error:"Evaluation failed."});}}
